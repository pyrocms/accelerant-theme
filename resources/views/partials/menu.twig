{% import "theme::macros/menu.twig" as menu %}

<aside id="menu" class="scrollbar">

    <ul>
        {% for section in template.cp.sections.root() %}
            {{ menu.section(template.cp.sections, section) }}
        {% endfor %}
    </ul>

</aside>

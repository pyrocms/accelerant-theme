{% import "pyrocms.theme.accelerant::macros/menu.twig" as menu %}

<aside id="menu" class="scrollbar">

    <ul>
        {% for section in template.cp.sections.root().visible() %}
            {{ menu.section(template.cp.sections, section) }}
        {% endfor %}
    </ul>

</aside>
